<!doctype html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title> {{pageTitle}}</title>
    </head>
    <body>
        {{> header}}
        <P>{{pageMessage}}</P>
        
        {{> footer}}
    </body>
</html>
